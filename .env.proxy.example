# Proxy-related environment example
# Copy/append these into your existing .env (do NOT delete existing secrets)

# Base domain for all subdomains (e.g. opendiscourse.net)
BASE_DOMAIN=example.com

# Optional: contact email for ACME certificate registrations
ACME_EMAIL=admin@example.com

# (Optional) Cloudflare token for DNS-01 challenge (if using Traefik or Caddy with DNS)
# Requires permissions: Zone:DNS:Edit for the target zone
CLOUDFLARE_API_TOKEN=cf_token_here

# Explicit hostnames (can be omitted; will default to <service>.<BASE_DOMAIN>)
PORTAL_HOSTNAME=portal.example.com
N8N_HOSTNAME=n8n.example.com
FLOWISE_HOSTNAME=flowise.example.com
WEBUI_HOSTNAME=openwebui.example.com
OLLAMA_HOSTNAME=ollama.example.com
SUPABASE_HOSTNAME=supabase.example.com
SEARXNG_HOSTNAME=searxng.example.com
LANGFUSE_HOSTNAME=langfuse.example.com
NEO4J_HOSTNAME=neo4j.example.com

# REVERSE_PROXY selection when using quickstart_proxy.sh (caddy_ext|traefik)
REVERSE_PROXY=caddy_ext
