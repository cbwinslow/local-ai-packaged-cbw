############
# N8N Configuration
############
N8N_ENCRYPTION_KEY=$(openssl rand -hex 24)
N8N_USER_MANAGEMENT_JWT_SECRET=$(openssl rand -hex 32)

############
# Supabase Secrets
############
POSTGRES_PASSWORD=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
JWT_SECRET=$(openssl rand -base64 32)
ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJhbm9uIiwKICAgICJpc3MiOiAic3VwYWJhc2UtZGVtbyIsCiAgICAiaWF0IjogMTY0MTc2OTIwMCwKICAgICJleHAiOiAxNzk5NTM1NjAwCn0.dc_X5iR_VP_qT0zsiyj_I_OZ2T9FtRU2BBNWN8Bu4GE
SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyAgCiAgICAicm9sZSI6ICJzZXJ2aWNlX3JvbGUiLAogICAgImlzcyI6ICJzdXBhYmFzZS1kZW1vIiwKICAgICJpYXQiOiAxNjQxNzY5MjAwLAogICAgImV4cCI6IDE3OTk1MzU2MDAKfQ.DaYlNEoUrrEn2Ig7tqibS-PHK5vgusbcbo7X36XVt4Q
DASHBOARD_USERNAME=supabase
DASHBOARD_PASSWORD=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
POOLER_TENANT_ID=$(openssl rand -hex 24)
POOLER_DB_POOL_SIZE=5

# Additional Supabase Configuration
POSTGRES_DB=postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POOLER_DEFAULT_POOL_SIZE=20
POOLER_MAX_CLIENT_CONN=1000
POOLER_PROXY_PORT_TRANSACTION=6432
KONG_HTTP_PORT=8000
KONG_HTTPS_PORT=8443
PGRST_DB_SCHEMAS=public,storage,graphql_public

# API Configuration
API_EXTERNAL_URL=https://supabase.opendiscourse.net
SITE_URL=https://supabase.opendiscourse.net
ADDITIONAL_REDIRECT_URLS=
STUDIO_DEFAULT_ORGANIZATION=Default
STUDIO_DEFAULT_PROJECT=Default

# Security Settings
DISABLE_SIGNUP=false
ENABLE_EMAIL_SIGNUP=true
ENABLE_EMAIL_AUTOCONFIRM=true
ENABLE_PHONE_SIGNUP=true
ENABLE_PHONE_AUTOCONFIRM=true
ENABLE_ANONYMOUS_USERS=true
JWT_EXPIRY=3600
FUNCTIONS_VERIFY_JWT=true

# Email Configuration
SMTP_ADMIN_EMAIL=admin@opendiscourse.net
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=user
SMTP_PASS=pass
SMTP_SENDER_NAME=OpenDiscourse
MAILER_URLPATHS_CONFIRMATION="/auth/v1/verify"
MAILER_URLPATHS_INVITE="/auth/v1/verify"
MAILER_URLPATHS_RECOVERY="/auth/v1/verify"
MAILER_URLPATHS_EMAIL_CHANGE="/auth/v1/verify"

############
# Neo4j Secrets
############   
NEO4J_AUTH=neo4j/$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)

############
# Langfuse credentials
############
CLICKHOUSE_PASSWORD=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
MINIO_ROOT_PASSWORD=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
LANGFUSE_SALT=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
NEXTAUTH_SECRET=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)
ENCRYPTION_KEY=$(openssl rand -hex 32)

############
# Flowise Configuration
############
FLOWISE_USERNAME=admin
FLOWISE_PASSWORD=$(openssl rand -base64 32 | tr -d '/+=' | head -c 32)

############
# Additional Configuration
############
DOCKER_SOCKET_LOCATION=/var/run/docker.sock
VAULT_ENC_KEY=$(openssl rand -hex 32)
SECRET_KEY_BASE=$(openssl rand -hex 64)
LOGFLARE_PUBLIC_ACCESS_TOKEN=$(openssl rand -hex 32)
LOGFLARE_PRIVATE_ACCESS_TOKEN=$(openssl rand -hex 32)

############
# Caddy Config
############
N8N_HOSTNAME=n8n.opendiscourse.net
WEBUI_HOSTNAME=openwebui.opendiscourse.net
FLOWISE_HOSTNAME=flowise.opendiscourse.net
SUPABASE_HOSTNAME=supabase.opendiscourse.net
OLLAMA_HOSTNAME=ollama.opendiscourse.net
SEARXNG_HOSTNAME=searxng.opendiscourse.net
NEO4J_HOSTNAME=neo4j.opendiscourse.net
LETSENCRYPT_EMAIL=blaine.winslow@gmail.com

############
# Image Proxy Configuration
############
IMGPROXY_ENABLE_WEBP_DETECTION=true

############
# Cloudflare Configuration
############
CLOUDFLARE_API_TOKEN=gKob2mQisfZo4JlzOmNct1VR9IEK0rn2rTFM4hH0
CLOUDFLARE_ZONE_ID=414bd44719253f65754a09cc4b60bd6f

############
# Supabase Public URL
############
SUPABASE_PUBLIC_URL=https://supabase.opendiscourse.net
